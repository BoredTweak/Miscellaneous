# Lombok Experimentation

A playground console application for experimenting with lombok and any other java libraries. 

## Local development

Run locally by running `./gradlew clean build` from a terminal instance in this directory.
