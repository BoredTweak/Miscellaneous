# Python Talking to Redis  

This project demonstrates communication between a Python application and a dockerized Redis instance. 

To run this locally, run `py app.py` in [the python-app directory](python-app).
